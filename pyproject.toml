[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "phlist-server"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0",
    "flask-limiter>=3.0",
]

[project.scripts]
phlist-server = "phlist_server:main"
